#include <bits/stdc++.h>
using namespace std;

int main(){
    /* 1.Å£Å£Ìø¸ß 
	int n, h, u;
    cin >> n >> h >> u;
    if(h >= u){
        cout << '0';
        return 0;
    }
    int t;
    vector<int> v;
    for(int i = 0; i < n; i++){
        cin >> t;
        v.push_back(t);
    }
    
	
	int count = 0;
    int target = u - h;
    int sum = 0;
    
    sort(v.begin(), v.end());
    
    for(int i = n - 1; i >= 0; i--){
        sum += v[i];
        ++count;
        if(sum >= target){
            break;
        }
    }
	cout << count;
	*/
	
	
	
}
